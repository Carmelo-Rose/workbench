# 交接文档 — 主图取景与比例（全部完成）

更新于 2026-08-05。阶段 1 提交于 `7f88132`（当时在 `feat/main-image-framing` 上），
**阶段 2 + 第 6/7/8 条已一并合并进 `main`，该分支已删除**。

> 工作区里还有**另一条线**（权限 / tenant / sidebar）的改动没提交，那不是这条线的东西，
> 合并时已逐文件挑选，没有把它带进去。

> 上一版交接文档讲的是 N1 任务中心 / N2 作品库 / 下一步 N3 用量面板，跟这条线不相干。
> 原文用 `git show 671cb5b:HANDOFF.md` 翻出来。**N3 用量面板仍然没做，优先级由用户定。**

## 结论先行

| # | 问题 | 状态 |
|---|---|---|
| 1 | `requestShadowBackdrop` 里 `.resize(w, h, { fit: "fill" })` 把生成图硬拉回原图尺寸，生图服务一旦对请求比例就近取整就是非等比拉伸 | ✅ 已删（雷，没爆过） |
| 2 | 主图裁切框在**白底图**上量、拿去裁**生成图**，两套坐标系；`a1af220` 的 `unionBox`/`centredOffset`/`clampOffset` 是在给这个错位打补丁 | ✅ 已删 |
| 3 | 「模型把帽子画窄了 6~7%」 | ❌ **这个问题不存在**，是量法造成的假象。为它写的自动拉伸已删，测量和告警留下。见下面「第 3 条是个假问题」 |
| 4 | 取景框 `measureInkBox` 把**投影**一起框进去，再把「商品+投影」居中——投影全打在一侧，商品就被顶到另一侧贴边 | ✅ 已修，`job_e1722cd4` 实跑验收通过 |
| 5 | 三个交付目录串在一条线上：images 的 8 次付费调用要排在主图的全部付费调用之后 | ✅ 阶段 2 已改成三支并行，用户已实跑一单 |
| 6 | 提亮基准 `GENERATED_SWEEP_WHITE_FLOOR` 拍成 245，实测底色是 253/254——全图乘 1.0408，吃掉 12%~35% 的投影 | ✅ 已改 253，见下面「提亮基准定错了」 |
| 7 | 商品占到 78%~90% 之后留白只剩 5%~11%，投影被画布挤出去 | ✅ fill ratio 改 `0.72/0.84`，投影追平「直接裁正方形」，见「第 7 条：留白」 |
| 8 | SKU 商品只占画布 73.6%，人工基准是 88%~98%——四分之一画布是白的 | ✅ `0.04/0.86` → `0.01/0.94`，见「第 8 条：SKU 留白」。**未实跑** |

**用户实际看到并否掉的那两版，根因都是第 4 条。**

## 第 4 条：取景（已修，已验收）

一张生成图里商品和投影**都只是"非白"，光看这张图分不开**。`measureInkBox` 的定义就是"所有非白像素的包围盒"，
所以它框的是「商品+投影」。把这个框往画布中间一摆，投影打在哪边、商品就被顶到另一边。

`job_14a3c460`（00:37 那版，被用户否掉的）实测，商品中心 X 本该 50%：

| 图号 | 中心 X | 占宽 | 左边距 | 右边距 |
|---|---|---|---|---|
| 4131 | 61.0% | 69.3% | 211px | 35px |
| 4132 | 66.9% | 57.0% | 307px | 37px |
| 4133 | 62.1% | 66.8% | 230px | 36px |
| 4134 | 57.1% | 77.8% | 146px | 32px |
| 4135 | 64.8% | 61.8% | 271px | 35px |

右边距恒等于 ~35px（正好是那 2% padding）＝商品一路顶到框的右沿。
「占宽」在 57%~78% 之间乱跳也是同一个原因：投影画多大，商品就被挤多小——**这才是"每版都不一样"的真身**，
不是比例问题。上一轮往提示词里加的"投影必须清晰可见"生效了、投影变大变实（4134 那张干脆是块硬边灰矩形），
反而把这个 bug 放大到一眼可见。

### 改法

抠图本来就有——当时那个 `correctedProportions` 为了量比例已经对生成图抠过一次，量完就扔。现在把商品的框留下来。

- `measureMatteAspect` → `measureMatteBox`：同一次扫描，除长宽比再返回商品的相对框。
- `correctedProportions` → `locateGeneratedArticle`，返回 `{ article, warning? }`。生成图的抠图挪到函数最前面，
  因为 `article` 现在是取景的必需品，不能因为原图抠图为空就提前 return 掉。
- `MasterRenderLayers.mainArticle: RelativeBox | null` 一路传到 `composeSquareDeliverable`。
- 新增 `frameArticleOnSquare`：**不再"先裁框再居中"**，改成「整张图等比缩放 → 取一个 800×800 的窗口、
  窗口中心对准商品中心 → 越界补白」。投影因此始终黏在商品旁边它本来的位置，越出画布就被裁掉，
  不再参与决定商品的位置和大小。缩放用 `.resize({ width })` 只给宽度、让 sharp 推高度，从根上保证等比。
- `squareFillRatio` 只吃一个商品长宽比，语义也变了：**「商品长边占画布多少」**（原来是「商品+投影+padding 的框占多少」）。
  常数跟着重定 `0.87/0.96` → `0.78/0.90`（**后又按第 7 条调到 `0.72/0.84`**），`SQUARE_MAIN_CROP_PADDING` 随之删除。
- `measureInkBox` 保留但降级为**兜底**：只在生成图抠图为空时走，同时报警告说"取景会被投影挤偏"。

**取景的目标值是从人工成品上量的**：`Z:\型麦-得物-品牌\【原图】-待制作\` 下的 `62604106\主图`、`XM2606013\主图`，
商品中心 X 在 45%~53%，商品占长边 69%~93%。

### 实跑验收（`job_e1722cd4`，01:14 落盘）

| 图号 | 中心 X | 中心 Y | 占宽 | 占高 | 左边距 | 右边距 | 投影像素占比 |
|---|---|---|---|---|---|---|---|
| 4131 | 49.9% | 51.1% | 90.1% | 68.9% | 39px | 40px | 4.11% |
| 4132 | 50.0% | 52.6% | 76.8% | 83.0% | 93px | 93px | 7.44% |
| 4133 | 50.0% | 51.2% | 85.3% | 73.4% | 59px | 59px | 7.77% |
| 4134 | 49.9% | 50.1% | 89.9% | 46.9% | 40px | 41px | 1.68% |
| 4135 | 50.0% | 49.9% | 88.0% | 70.4% | 48px | 48px | 6.04% |

左右边距逐张对称，中心 X 全在 49.9%~50.0%，占长边 76.8%~90.1%（人工基准 69%~93%），投影都还在。
中心 Y 那 1~2.6% 的偏是**量法差异**，不是取景差异：代码用抠图定位、这张表用色度阈值，两者对帽子内衬那种浅灰的判定不同。

**已知取舍**：商品放到 78%~90% 之后留白只剩 5%~11%，**一侧拖很长的投影会被画布裁掉一部分**。
想多留投影只有一个旋钮——调小那两个 fill ratio。**第 7 条就是拧了这个旋钮**（→ `0.72/0.84`）。

## 第 3 条是个假问题（重要，别再追了）

两个会话追了"模型把帽子画窄 6~7%"这个数，**它是验证脚本量出来的假象**。

那个脚本判定商品像素用 `max<200 || 色度>40`。**原图里那片投影是实打实的深灰，`max` 落在 160~200 之间，
被算成了商品**，于是原图的包围盒被投影撑宽了；而生成图的投影更浅（还被 `GENERATED_SWEEP_WHITE_FLOOR`
提亮过），落在 200 以上、没被算进去。拿"帽子+投影"跟"帽子"比宽度，当然差 6~8%。

4131 的阈值敏感度（同一对图，只改判定阈值 T）：

| T | 原图长宽比（框） | 主图长宽比（框） | 偏离 |
|---|---|---|---|
| 200 | 1.421 (405×285) | 1.310 (541×413) | **-7.8%** |
| 160 | 1.310 (368×281) | 1.317 (540×410) | +0.6% |
| 120 | 1.319 (368×279) | 1.324 (540×408) | +0.3% |
| 90 | 1.329 (368×277) | 1.328 (539×406) | -0.1% |
| 70 | 1.333 (368×276) | 1.337 (539×403) | +0.3% |

原图的框从 T=200 到 T=160 宽度掉了 37px（-9%）、高度只掉 4px——**掉的全是横向，就是旁边那片投影**。
主图的框 541→540，几乎不动。T≤160 之后两边一致到 ±0.6%。

最直接的证据：把两张图各自裁到帽子（T=90）、缩到同高 420px，
**原图 560px 宽，主图 560px 宽，一模一样**（4133 是 498 vs 500）。
（重现：项目根目录写个 `.mjs`，两张图各自按 T=90 求包围盒 → `extract` → `resize({height:420})` → 比宽度。）

推论：交接文档里"原图 1.421"这个基准值从头就是错的，**1.33 才是这顶帽子的真实长宽比**。
前几轮记的 -6.5% / -6.8% 也都是同一个假象。

### 处置：拆掉执行器，留下传感器（已做）

用户让我定，定的结果是**删掉自动拉伸、保留测量和告警**。

- 删除 `planProportionRepair` / `ProportionRepair` / `correctedProportions` /
  `MIN_PROPORTION_CORRECTION` / `MAX_PROPORTION_CORRECTION`，以及那句 `resize(..., { fit: "fill" })`。
  **本文件现在没有任何一处会改变商品长宽比的操作。**
- 新增纯函数 `describeProportionDrift(sourceName, sourceAspect, generatedAspect)`：
  漂移超过 `ARTICLE_PROPORTION_TOLERANCE`（5%）就返回一条告警文案，否则返回 `undefined`。
  5% 这个值：探针噪声够不着，又能抓住当初以为存在的那个 ~6% 重画。
- `correctedProportions` → `locateGeneratedArticle`，**不再改图**，只返回
  `{ article, warning? }`。`makeWhiteMaster` 直接用 `generated`，后面只剩提亮那一步。

**为什么是这个方向**：两种误判不对称。告警误报，代价是有人多看一眼好图；拉伸误报，
代价是发出去一张变形的商品图、而且下游没有任何东西能发现——**那正是这条线最开始的投诉本身**。
既然触发它的证据已经证明是量错了，就不该让代码继续凭一个抠图读数自作主张动图。

**注意 `measureMatteBox` 不能删**——主图取景现在靠它定位商品。
生成图的那次抠图调用也**没省下**：它现在是取景的必需品，不是比例校正的附属品。

## 第 6 条：提亮基准定错了（已修）

用户实跑阶段 2 之后报「裁剪完阴影变了」，并把未裁剪的生成图放到
`Z:\型麦-得物-品牌\【详情页】-待审\1234\原生成图` 做对照。**他是对的，但凶手不是裁剪。**

`frameArticleOnSquare` 只做等比缩放和取窗口，不碰任何像素值——逐点验证过：留在画布内的
阴影像素，主图实测值 = 原图值 × 255/245，误差 ±2。动阴影的是它**上游**那一步全局提亮
（`renderMainFrame` 里的 `.linear(255 / GENERATED_SWEEP_WHITE_FLOOR, 0)`，从阶段 1 之前就在）。

**基准值是拍的，不是量的。** 实测 5 张生成图，背景底色是 **253/254（占全图 ~58%）**，
245-252 那一档只有 ~2.7%、全是投影的外圈过渡。而代码按 245 算，等于全图乘 1.0408：

| 原图灰度 | floor=245（原） | floor=253（现） |
|---|---|---|
| 254（背景） | 255 ✅消接缝 | 255 ✅消接缝 |
| 245（投影外圈） | **255 — 整层没了** | 247 |
| 230 | 239 | 232 |
| 200 | 208 | 202 |

可见投影像素（200-249）实测，同一批图、同一个取景窗口：

| 图号 | 完全不提亮 | floor=253 | floor=245（实际产出） |
|---|---|---|---|
| 4131 | 6.22% | 6.11% | **3.98%** |
| 4132 | 9.40% | 9.03% | **6.30%** |
| 4133 | 8.93% | 8.83% | **6.86%** |
| 4134 | 2.32% | 2.25% | **1.57%** |
| 4135 | 5.64% | 5.43% | **4.78%** |

253 只比「完全不提亮」少 1%~4%，比 245 多留 12%~35%，而四角照样 255、纯白占比 57% vs 58%——
**接缝照样消掉**。完全不提亮不行：背景 254 贴着纯白补白区会露出方块边界（4132 左下角一眼可见）。

### 改法

- `GENERATED_SWEEP_WHITE_FLOOR` 245 → **253**，注释改成记录实测底色和「这个值必须贴着实测量，
  因为 `v * 255 / floor` 作用于整张图，每往下调一档投影就跟着被乘一次」。
- 提亮那三行从 `renderMainFrame` 里提成导出的 `liftGeneratedSweep(frame)`。
  **理由是它值得有个名字**：主图链路上只有这一步会改变生成图的像素值，下游全是缩放和裁切。
  以后再出现「阴影跟生成图不一样」，第一个该看的就是它。
- 新增测试 `generated sweep lift`：一张背景 254 + 投影 245/230 + 商品 20 的图，断言背景提到 255
  且其余三档都不许跟着跑。**这条尺子验过**——把常数改回 245 它立刻红（`expected 255 to be less than 250`）。

## 第 7 条：留白（已改）

用户问「能不能直接对着生成图裁 1:1，不要那些花里胡哨的」。**试了，答案是不能——但他要的东西
用一个常数就能拿到。**

### 直接裁实测（`直接裁1比1/`，纯裁 1023×1023 对准商品中心再缩到 800，不提亮不补白）

投影确实更全（4133 翻倍），构图更松。但有两个问题，第二个是硬伤：

1. **商品大小 65.8%~100% 乱跳**，完全由模型这次把帽子画多大决定。
2. **4134 直接废**：那张帽子横着 1106px，比生成图短边 1023px 还宽，左边帽扣、右边帽檐被画布切掉，
   投影全在画布外（2.05%，比现状还少）。**生成图是 3:2 横构图，裁正方形必然砍横向**，调参救不了。

### 改法：拧 fill ratio，不是换裁法

`0.78/0.90` → **`0.72/0.84`**，取景算法一个字没动。三版对比（同一批生成图）：

| 图号 | 占长边（0.72/0.84 / 直接裁 / 0.78/0.90） | 投影占比（同序） |
|---|---|---|
| 4131 | 83.9% / 97.9% / 89.9% | 6.47% / 7.17% / 5.79% |
| 4132 | 72.0% / 65.8% / 78.8% | **9.34%** / 8.83% / 8.25% |
| 4133 | 78.8% / 80.0% / 85.4% | 6.22% / 7.69% / 3.71% |
| 4134 | 84.0% / **100%（切了）** / 90.0% | **2.44%** / 2.05% / 1.33% |
| 4135 | 81.6% / 81.0% / 87.5% | 5.97% / 6.36% / 4.66% |

**投影追平直接裁**（4132、4134 还更多），商品大小收在 72%~84%（直接裁 65.8%~100%），4134 完整。

关键在于**两者拿到的是同一个东西**：画布边缘多留一圈，投影就保住了。区别只是 ratio 版的那一圈
是算出来的（每张都留够），裁版的是碰运气（模型画小了才有，画太大还切商品）。这就是
「why a ratio and not a crop」，注释里记着。

> 对照目录留在 `Z:\型麦-得物-品牌\【详情页】-待审\1234\` 下：`直接裁1比1`、`对照-留白多一点`，
> **用完可删**。它们是探针脚本重切的，不是流水线产出（`对照-留白多一点` 用的是真实取景算法 +
> 253 提亮，唯一差别就是那两个常数，所以它约等于改完之后的产出）。

## 第 8 条：SKU 留白（已改，未实跑）

用户提的第二件事：「SKU 不要这么多留白」。量完发现差得比主图那条还多。

| | 商品占长边 | 最窄边距 |
|---|---|---|
| 现有产出 `SKU1.png` | **73.6%** | 13.0%（四边 107/104/153/155） |
| 人工 `62604106`（5 张） | 88.3%~89.8% | 4.8%~5.6% |
| 人工 `XM2606013/SKU`（7 张） | 91.1%~98.1% | 0%~4.1% |

### 为什么写着 0.86 却印成 73.6%

**两个常数是相乘的，而 `SQUARE_SKU_CROP_PADDING` 是相对源图、不是相对商品。**
`cropBufferToBox` 按 `(box.left - padding) * width` 往外扩，白底图上商品长边只占约 47.5%，
每边扩 4% 就等于把裁剪框撑大 17%，商品在框里只剩 85.6%——`0.86 × 0.856 = 0.736`，正是实测值。

改成 `0.01 / 0.94`，同一个算式给出 `0.94 × 0.9596 ≈ 90%`，落在人工基准的中段。
padding 不归零是因为它还有一个正经用处：防止裁进抠图掩膜自己的边。

**这条没实跑过**——1234 那单的 staging 已经被清理（`rm(masterStage)` 在三支 settle 之后），
没有白底图可以重切验证，只有算式和人工基准两个依据。下次跑一单量一下商品占长边，
应该在 88%~92%，明显超出就回来调 `SQUARE_SKU_FILL_RATIO`。

## 阶段 2（已完成，已实跑）

形态按原定方案：**一个 job 内三条并行分支，没有拆成三个独立任务**。

### 真正被解开的那个串行

原来 `makeWhiteMaster` 一个函数里同时干了抠图（本地）和 `requestShadowBackdrop`（付费）。
它对**每一张原图**都跑一遍，**全跑完**才轮到聚类，聚类完才轮到 images 那 8 次付费模特图。
也就是说：**images 的付费调用要排在主图的全部付费调用后面**，两段慢活串着跑。

现在拆成「共享前缀 + 三支并行」：

```
共享前缀（全本地）：每张原图 → 抠图 → 白底图 → 前景 + metric  →  聚类
                                    ↓
        ┌───────────────────────────┼───────────────────────────┐
      主图支                       SKU 支                    images 支
  付费生背景 → 定位 → 取景        本地合成                8 张付费模特图 + 4 张本地拼版
       ↓                            ↓                            ↓
   发布 主图/                   发布 SKU/                   发布 images/
```

`Promise.all` + 逐支 try/catch（`runPipelineBranches`），任一支失败其余照常发布。

**代价**：主图的付费生成不再和抠图重叠了（原来在同一个 `Promise.all` 里）。抠图是本地 GPU、
现在很快，丢掉的重叠是秒级；换来的是 images 不用等主图的付费调用，量级是分钟。

### 主图选图脱钩（原以为要做产品决策，结论是不用）

交接文档原来说"哪几张原图产出主图由 `classification.colors.flatMap(c => c.members)` 决定，
改它等于改产出文件集合，属于产品决策"。**实际不用做这个决策**：

`classifySources` 里 `colors` 的成员就是 `usable ∧ edgeRatio ≤ 阈值` 这一组，聚类只是把它们分堆、
不丢任何一张（`clusterByColor` 是全划分）。所以把这个判据提成一个纯函数
`isFullArticleShot(metric)`（`product-classify.ts`），主图用它逐张自己判，**产出集合和原来逐字节相同**，
但不再需要等全文件夹的抠图跑完才知道该给哪几张出主图。

`classifySources` 内部也改用同一个函数，`product-classify.test.ts` 新增一条测试锁住
"predicate 选出来的集合 === 聚类出来的成员集合"——这两个一旦漂开，主图就会**悄悄**换一批图发布。

> 如果**想**改这个集合（比如让所有非 `x_` 原图都出主图），那才是产品决策，本次没做。
>
> 顺带修掉的一个浪费：**改之前，被判成 detail 的原图也在花钱生成主图背景，生成完直接丢掉**——
> `makeWhiteMaster` 是对每一张 `articleSources` 跑的，筛选发生在它之后。现在筛选在付费之前。

### 进度上报

`progress()` 换成 `createPipelineProgress(emit, activeBranches)`：

- **合并快照**。`updateMonoJobResult` 是整块覆盖 `result_json` 的，三支并发各报各的会互相抹掉字段，
  所以累积字段现在由 reporter 持有，每次都发完整快照。
- **加权汇总**：`prepare .2 / main .3 / sku .05 / images .45`，总进度是加权和，不再是单调百分比。
- **`stage` 取关键路径**——剩余权重最大的那支。**没人报过的支（stage 仍是 `queued`）不参选**，
  否则前缀阶段整块时间卡片都会显示 images 的"排队中"。
- 失败的支 `settle` 到 100，免得它把总进度永远吊在 100 以下。
- 每支的 stage/progress 另放在 `result.branches` 里，卡片多渲染一小块逐支进度。

UI 两处：`ProductPipelineCard.tsx` 和 `src/app/try/product-pipeline/ProductPipelineTrial.tsx`
（新 stage 文案 + 逐支进度 + `failedBranches` 提示）。结果里新增 `failedBranches`，
`incomplete` 现在也包含"整支没发出来"。

### 边界（未变）

`images` 内部分工不变（11 个槽位里只有 8 张模特图走大模型，`02/09/10/11` 继续本地模板拼版）；
**颜色聚类保留**，只是主图不再依赖它。完整计划见
`C:\Users\Administrator.DESKTOP-GRHN4PA\.claude\plans\sku-foamy-map.md`。

### 实跑（用户已跑一单，2026-08-05 18:22 落盘到 `1234/`）

**主图取景逐张对上了阶段 1 的数字**，三支并行没有改变产出。下表按 T=90（只算很暗的帽子像素）
量帽子框，和阶段 1 那两张表是同一批图：

| 图号 | 中心 X | 占宽 | 阶段 1 占宽（`e1722cd4` / `867a89e9`） |
|---|---|---|---|
| 4131 | 50.0% | 90.3% | 90.1% / 90.3% |
| 4132 | 49.9% | 75.6% | 76.8% / 76.6% |
| 4133 | 50.0% | 84.3% | 85.3% / 85.4% |
| 4134 | 50.0% | 90.3% | 89.9% / 90.3% |
| 4135 | 50.0% | 87.5% | 88.0% / 87.6% |

**还没验的**：`SKU/`、`images/` 两个目录的内容没有逐张比对；卡片上三支进度条是否各走各的、
总进度是否不倒退没看；「故意把生图 API key 配错，`SKU/` 照常发布 + `failedBranches`」这条没跑。

**第 6 条（提亮 253）已实跑**——用户 18:58 又跑了一单，判据是生成图里 245-252 那层在主图里
留下了 1.0%~3.3%（245 版会把它整层推成 255，应该是 0）。那单投影占比 1.33%~8.25%，取景数字没变。

**第 7、8 条改完之后还没跑过。** 下次跑完量三件事：主图商品占长边应在 72%~84%、
投影比 18:58 那单多、SKU 商品占长边应在 88%~92%。

## 其他还开着的口子

- **4134 的投影被模型画成一块硬边灰矩形**。取景修完之后它不再影响构图，但"画得像不像投影"仍然
  完全由模型决定，本地没有任何兜底或校验。
- **`job_14a3c460` 那次卡死**：standalone worker 进程死了没人回收租约。那条线后来自己收口了，
  见 `d68e550`（terminalize product jobs whose worker vanished）和 `bb85f5e`。
- **工作区里有另一条线在同时改动**（权限 / tenant / sidebar 一带）。本次会话期间就看到
  `tenant.ts`、`threadlist-sidebar.tsx` 被改出 tsc 错误。**判断"全仓是否绿"之前先确认那条线的状态**，
  别把它的红算到这条线头上。

## 回归验证方式

```bash
npx tsc --noEmit
```

```bash
npx vitest run
```

```bash
npx eslint src/lib/mono/product-pipeline.ts src/lib/mono/product-pipeline.test.ts src/lib/mono/product-classify.ts src/lib/mono/product-classify.test.ts src/components/workbench/ProductPipelineCard.tsx src/app/try/product-pipeline/ProductPipelineTrial.tsx
```

第 6 条修完后：vitest **275 passed**（32 个文件全绿）；`product-pipeline.ts` / `.test.ts` eslint 干净。

**tsc 还剩 1 处报错，不是这条线的**——`src/lib/server/tenant.test.ts`（`WorkspaceActor` 缺 `traceId`）。
原来还有一处 `src/components/assistant-ui/threadlist-sidebar.tsx`（JSX 括号没配对），
**已被那条线自己修掉**。这两个文件都属于工作区里并行的另一条线（权限 / tenant / sidebar）。
本条线动过的文件 tsc 干净。**先确认那条线收工，再判断全仓 tsc 状态。**

第 6/7/8 条只动了 `src/lib/mono/product-pipeline.ts`（提亮基准 + `liftGeneratedSweep` +
主图 fill ratio + SKU 那两个常数）和 `src/lib/mono/product-pipeline.test.ts`（新增 `generated sweep lift`）。
第 7、8 条**没有新增测试**：现有测试锁的是「商品居中」「同一商品配不同投影产出必须一致」这些契约，
而这三个常数是要按人工基准调的旋钮，把具体数字写进断言只会挡住下一次调参。

阶段 2 动了这些文件：

- `src/lib/mono/product-pipeline.ts` — 主战场
- `src/lib/mono/product-classify.ts` — 新增 `isFullArticleShot`
- `src/lib/mono/product-pipeline.test.ts` / `product-classify.test.ts`
- `src/components/workbench/ProductPipelineCard.tsx`
- `src/app/try/product-pipeline/ProductPipelineTrial.tsx`

> `job_e1722cd4` 的验收是在**删拉伸之前**跑的。删掉之后主图链路少了一次条件性的单轴 resize。
> 用外部探针量到的漂移是 ±0.6%，低于当时 1% 的下限，**大概率本来就没触发拉伸**；
> 但代码内部用的是抠图掩膜、不是这个探针，那次到底触发没触发从产出上看不出来。

### 删拉伸之后的复跑（`job_867a89e9`，2026-08-05 01:47 落盘）

只跑主图/SKU（`PRODUCT_PIPELINE_SHADOW_ONLY_TRIAL` 临时置 true，跑完已改回 false）。同一个探针、同一批原图：

| 图号 | 中心 X | 占宽（新 / `job_e1722cd4`） | 左/右边距 |
|---|---|---|---|
| 4131 | 49.9% | 90.3% / 90.1% | 39 / 39px |
| 4132 | 50.0% | 76.6% / 76.8% | 94 / 93px |
| 4133 | 49.9% | 85.4% / 85.1% | 58 / 59px |
| 4134 | 49.9% | 90.3% / 89.6% | 39 / 39px |
| 4135 | 50.0% | 87.6% / 88.0% | 50 / 49px |

**取景没变**（占宽最大差 0.7pp，是模型每次重画投影带来的噪声，不是取景差异），
warnings 里既没有比例漂移也没有 `MAIN_FRAME_ASPECT_TOLERANCE` 告警。删拉伸这件事对产出无影响，这条口子可以关掉。

测试都在 `src/lib/mono/product-pipeline.test.ts`：

- `square deliverable composition`：主图缩放必须等比（2:1 的块出来还得是 2:1）；主图带的投影不能被当成独立图层丢掉。
- 取景三条，共用夹具 `litFromOneSide(reach)`（商品在右、投影往左拖 `reach` 那么远）：
  「按商品居中，不按商品+投影居中」（中心须落在 392~408px）；
  「同一个商品配两种长度的投影，产出的商品框必须逐像素相等」（盯"每版都不一样"）；
  「抠图为空时仍然出图」（锁兜底分支不抛异常）。
- `主图 article measurement`：`measureMatteAspect` 的灰度/RGBA 两种掩膜、空掩膜返回 null；
  `describeProportionDrift` 三条——容差内不吭声（含 1234 的真实读数 1.333/1.332、1.186/1.192）、
  超容差要报出图名和两个读数、以及**两个方向都只返回字符串，没有 resize 可交回**。
- **删了一条**：原来那条"给主图一个指向错误区域的 box 也要框对"。它的前提是"传进来的 box 一定来自另一张画、不可信"，
  而现在主图的 box 就是这张生成图自己抠出来的，前提不成立了。**不是因为它变红才删的，是它描述的契约已经不存在。**

阶段 2 新增（同一个文件）：

- `parallel deliverable branches` 三条：一支挂了另两支照跑完；失败按**声明顺序**上报（不是发生顺序）；
  settle 回调每支恰好一次、带原因。
- `aggregated branch progress` 六条：加权汇总而不是每支各刷一遍进度条；一支设的字段不被另一支的上报抹掉
  （这是 `updateMonoJobResult` 整块覆盖带来的真实风险）；`stage` 取关键路径；
  **没人报过的支不许当 headline**（否则前缀阶段整块显示"排队中"）；失败的支不许把总进度永远吊住；单支不许倒退。
- `product-classify.test.ts`：`isFullArticleShot` 选出的集合必须**逐元素等于**聚类出来的成员集合。
  这条是主图脱钩的全部依据，它一红就说明主图开始悄悄发布另一批图了。

## 踩过的坑

**最大的一个：先验证你的尺子，再追你量出来的数。** 第 3 条被追了两个会话，
根源是验证脚本的一个阈值把原图的投影算成了商品。判据很简单——**换个阈值再量一遍，
数不该跟着阈值跑**。一跑阈值敏感度就露馅了。

**同一族的第二个坑：常数要量出来，不要拍。** `GENERATED_SWEEP_WHITE_FLOOR = 245` 从来没人量过
生成图的底色到底是多少（实测 253/254），于是它一边完成了消接缝的本职，一边偷偷把每张主图的投影
乘淡了 4%。它和第 3 条的共同点是**拿一个没验过的数当基准**，差别只在第 3 条的错数来自脚本阈值、
这条的错数来自直接拍脑袋。判据一样：**任何一个「图像应该长什么样」的常数，都要能说出它是从哪张图上量的。**

**测试里用 sharp 造掩膜，`composite()` 会自动加 alpha 通道**，于是 `metadata().hasAlpha` 为 true，
`measureMatteBox` 走"legacy RGBA"分支去读 alpha——而那个 alpha 全不透明，量出来的包围盒等于整张画布。
造灰度掩膜必须显式 `.removeAlpha()`。另外 `sharp({create:{channels:1}})` 直接报错，create 只接受 3 或 4 通道。

**`fetch` 的 body 不接受 `Buffer`**。`requestCutout` 原来能过是因为 `readFile()` 返回 `Buffer<ArrayBuffer>`；
一旦提成 `bytes: Buffer` 参数就变成 `Buffer<ArrayBufferLike>`，TS 拒绝。现在包一层 `new Uint8Array(bytes)`。

**并发上报最容易踩的是"整块覆盖"**。`updateMonoJobResult` 直接 `SET result_json = ?`，
串行时代靠每个调用点自己把该带的字段重新传一遍来维持；三支并发之后这招必然互相抹掉。
判据：**只要有两个地方可能同时上报，累积状态就必须收到一个地方去，调用点只报增量。**

**并发做完之后，`rm(masterStage)` 的位置是有讲究的**。白底图不只是喂聚类，
images 支的详情页在没有 `x_` 特写时会回落到读 `classification.details[].path` / `colors[0].members[].path`，
那些都是白底图路径。所以清理必须在**三支全部 settle 之后**，不能塞进前缀末尾。

**sharp 一条链子里同时用 `extend()` 和 `extract()` 要小心**：sharp 按固定阶段跑，
`extract` 还分"resize 前"和"resize 后"两种。`frameArticleOnSquare` 里把补白和取窗口拆成两个 pass、
中间落一次 Buffer，就是不想赌这个顺序。

**临时脚本要放在项目根目录跑**，放 scratchpad 里 `import sharp` 解析不到（ESM 不吃 NODE_PATH）。
后缀也得挑：`.ts` 会被 `npx tsx` 当 cjs 处理、顶层 `await` 直接报错；要 import 真实 pipeline 函数就写 `.mts`，
纯 sharp 脚本写 `.mjs`。用完记得删。
